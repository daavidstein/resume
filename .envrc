# Load local secrets if present (do not commit .envrc.local with real keys).
if [ -f .envrc.local ]; then
  source_env .envrc.local
fi

# Optional defaults for this repo.
export RESUME_SB_EMBEDDING_BACKEND="${RESUME_SB_EMBEDDING_BACKEND:-openai}"
